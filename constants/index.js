// Central export for all constants
const { ERRORS, SUCCESS, CONSOLE } = require('./messages');
const { QUERIES } = require('./queries');
const { CONFIG } = require('./config');

module.exports = {
  ERRORS,
  SUCCESS,
  CONSOLE,
  QUERIES,
  CONFIG,
};

